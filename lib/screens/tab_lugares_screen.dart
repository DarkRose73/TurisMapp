// ignore_for_file: prefer_const_constructors

import 'package:flutter/material.dart';

class TabLugaresScreen extends StatelessWidget {
  const TabLugaresScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'lugares screen',
        style: TextStyle(
          fontSize: 30,
        ),
      ),
    );
  }
}
